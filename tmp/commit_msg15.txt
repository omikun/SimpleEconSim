Add tech-stack & engine section to GDD + client/infra action items

- gdd.md §13b "Tech stack & engine": Python sim server + thin game
  client architecture (intents only), engine comparison table
  (Godot 4 recommended; Unity final option; web demo), 2D graphics
  style with density rendering + hero agents, CC0 asset sources
  (Kenney/OpenGameArt/itch/Google Fonts), 6-step MVP path, and
  design-time gotchas (rendering scale, determinism/PYTHONHASHSEED,
  multiplayer via authority-server).
- priority_tasks.md: add cross-cutting items C.4 (JSON world-state +
  intent schema client contract), C.5 (sim_server.py headless
  wrapper), C.6 (determinism pin: PYTHONHASHSEED + replace per-process
  hash in region.py before multiplayer), C.7 (feasibility client:
  Godot 2D or web+ECharts), C.8 (rendering spec: density + hero
  agents), C.9 (CC0 asset pack selection).